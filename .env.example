# FB_PROFILE sets the name of the profile folder in data/profiles/
FB_PROFILE=Automation_1

# Path to FFmpeg binary (leave as 'ffmpeg' if in PATH)
FFMPEG_PATH=ffmpeg

# Affiliate Settings (Optional)
DEFAULT_AFFILIATE_LINK=https://shope.ee/your_default_id
